# Seed Notes

To run the MVP benchmark seed in Supabase:

1. Open the Supabase SQL Editor for the project.
2. Paste the contents of `supabase/seed/benchmarks_mvp_d1mid_d2.sql`.
3. Run the query.
4. (Optional) Paste and run `supabase/seed/benchmarks_mvp_validate.sql` to verify counts and coverage.
